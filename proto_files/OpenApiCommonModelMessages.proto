syntax = "proto3";

package OpenApi.CommonModel;

enum ProtoErrorCode {
  UNKNOWN_ERROR = 0;
  INVALID_TOKEN = 1;
  INVALID_ACCOUNT = 2;
  INSUFFICIENT_FUNDS = 3;
  ORDER_REJECTED = 4;
  SYMBOL_NOT_FOUND = 5;
}
